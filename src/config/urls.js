const baseURL = 'https://jsonplaceholder.typicode.com';
const carURL = 'http://owu.linkpc.net/api/v2'

const urls = {
    users: '/users',
    posts: '/posts',
    cars: '/cars'
}

export {
    baseURL,
    urls,
    carURL
}